{% extends '::layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/stepList.css') }}" type="text/css">        
{% endblock %}

{% block content %}
    <h1>My Steps</h1>
    Create an account to track the steps you take here.
    <div class="actions">
            <a class="btn large primary" href="{{ url('fos_user_registration_register') }}">Create an Account</a>
            or 
            <a class="btn large" href="{{ url('fos_user_security_login') }}">Log in</a>
    </div>    
{% endblock content %}