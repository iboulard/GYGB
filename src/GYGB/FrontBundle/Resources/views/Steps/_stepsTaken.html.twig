<div class="" id="steps-taken" style="margin: 0 0 20px 16px; float: left; width: 48%;">
    {# <h1>Steps Taken</h1> #}

    <a href="{{ path('steps') }}">
    <div id="step-summary" style="float: left;">
        <div id="steps-taken-counter">
            {% for c in stepDigits %}<span class="button step-digit {% if loop.last %}last{% endif %} ">{{ c }}</span>{% endfor %}<span class="button step-digit hidden" style="display: none;">6</span>
            Steps Taken
        </div>
    </div>
    </a>

</div>
